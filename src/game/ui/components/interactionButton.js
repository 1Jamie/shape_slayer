(function () {
    // Disabled: the canvas-rendered interaction button in js/ui.js is the
    // canonical mobile action button. Keeping this DOM copy active creates a
    // duplicate top-screen prompt.
})();







